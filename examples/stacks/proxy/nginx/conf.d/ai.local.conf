# TLS defaults
ssl_session_cache shared:SSL:10m;
ssl_session_timeout 10m;
ssl_protocols TLSv1.2 TLSv1.3;
ssl_prefer_server_ciphers off;

server {
    listen 80;
    listen 443 ssl http2;
    server_name ai.local;

    ssl_certificate     /etc/nginx/certs/ai.local.crt;
    ssl_certificate_key /etc/nginx/certs/ai.local.key;

    root /usr/share/nginx/html;
    index index.html;
    client_max_body_size 200M;

    location / {
        try_files $uri /index.html;
    }
}

server {
    listen 80;
    listen 443 ssl http2;
    server_name llama.ai.local;

    ssl_certificate     /etc/nginx/certs/ai.local.crt;
    ssl_certificate_key /etc/nginx/certs/ai.local.key;

    location / {
        include /etc/nginx/snippets/proxy-common.conf;
        proxy_pass http://host.docker.internal:11434;
    }
}

server {
    listen 80;
    listen 443 ssl http2;
    server_name openwebui.ai.local;

    ssl_certificate     /etc/nginx/certs/ai.local.crt;
    ssl_certificate_key /etc/nginx/certs/ai.local.key;

    location / {
        include /etc/nginx/snippets/proxy-common.conf;
        proxy_pass http://host.docker.internal:3000;
    }
}

server {
    listen 80;
    listen 443 ssl http2;
    server_name searxng.ai.local;

    ssl_certificate     /etc/nginx/certs/ai.local.crt;
    ssl_certificate_key /etc/nginx/certs/ai.local.key;

    location / {
        include /etc/nginx/snippets/proxy-common.conf;
        proxy_pass http://host.docker.internal:8080;
    }
}

server {
    listen 80;
    listen 443 ssl http2;
    server_name comfyui.ai.local;

    ssl_certificate     /etc/nginx/certs/ai.local.crt;
    ssl_certificate_key /etc/nginx/certs/ai.local.key;

    client_max_body_size 200M;

    location / {
        include /etc/nginx/snippets/proxy-common.conf;
        proxy_pass http://host.docker.internal:8188;
    }
}
